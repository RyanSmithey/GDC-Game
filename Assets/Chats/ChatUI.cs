using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class ChatUI : MonoBehaviour
{
    [SerializeField] private GameObject StartChatPlayerButton;
    [SerializeField] private GameObject OngoingChatButton;
    [SerializeField] private GameObject NewChatButton;

    // Start is called before the first frame update
    void Start()
    {
        //Request Existing Chat Logs based on player from server
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
